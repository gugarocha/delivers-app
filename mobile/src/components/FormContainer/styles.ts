import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFF',
    marginVertical: 30,
    marginHorizontal: 8,
    paddingHorizontal: 20,
    paddingBottom: 30,
    borderRadius: 8,
    elevation: 5,
  },
});